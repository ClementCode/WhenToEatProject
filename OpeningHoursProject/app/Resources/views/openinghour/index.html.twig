{% extends 'base.html.twig' %}

{% block body %}
    <h1>Liste des horaires</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Hours</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for openingHour in openingHours %}
            <tr>
                <td><a href="{{ path('openinghour_show', { 'id': openingHour.id }) }}">{{ openingHour.id }}</a></td>
                <td>{{ openingHour.hours }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('openinghour_show', { 'id': openingHour.id }) }}">Voir</a>
                        </li>
                        <li>
                            <a href="{{ path('openinghour_edit', { 'id': openingHour.id }) }}">Modifier cet horaire</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('openinghour_new') }}">Créer un nouvel horaire</a>
        </li>
    </ul>
{% endblock %}
